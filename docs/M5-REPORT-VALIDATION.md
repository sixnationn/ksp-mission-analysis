# M5 study report checkpoint

`StudyReport` saves and reloads version 1 JSON with the source hash/confidence, exporter/game/save/capture and installed-mod identity, Principia state/frame transform provenance, explicit inertial frame and epoch, independent ephemeris settings and fit evidence, named body roles, SI time grids, no-leap display calendar, and ranked patched-conic routes. Runtime-labeled reports require a lowercase 64-character source hash. Reload rejects duplicate route IDs, provenance changes, unsupported validation labels, altered burn sums, mismatched leg roles/dates, unsafe flyby margins, nonfinite values and incomplete JSON. Save validates before writing and uses a synced temporary file in the destination directory followed by replacement.

The tests failed at the missing `StudyReport.hpp` before implementation, then at the missing runtime composer and mod provenance validation. The Windows MSYS2 UCRT64 focused executable now passes **28 checks**, including a synthetic round trip, exact-byte runtime-shaped composition, worker-route conversion, and rejection that preserves the previous file. All six Ubuntu and Windows jobs passed in [CI run 35797442290](https://github.com/sixnationn/ksp-mission-analysis/actions/runs/35797442290) for the runtime composer and GTK save action; the mod provenance addition awaits its next CI run. This is headless file-format evidence; no GTK save action or real runtime export has been visually verified yet. A saved report remains a historical screened study, not a refreshed KSP observation or a Principia-equivalent result.
