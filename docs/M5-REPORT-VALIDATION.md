# M5 study report checkpoint

`StudyReport` saves and reloads version 1 JSON with the source hash/confidence, explicit inertial frame and epoch, independent ephemeris settings and fit evidence, named body roles, SI time grids, no-leap display calendar, and ranked patched-conic routes. Runtime-labeled reports require exporter/game/save/capture identity and a lowercase 64-character source hash. Reload rejects duplicate route IDs, provenance changes, unsupported validation labels, altered burn sums, mismatched leg roles/dates, unsafe flyby margins, nonfinite values and incomplete JSON. Save validates before writing and uses a synced temporary file in the destination directory followed by replacement.

The tests failed at the missing `StudyReport.hpp` before implementation. The Windows MSYS2 UCRT64 focused executable now passes **21 checks**, including a synthetic round trip, a runtime-shaped provenance case, and rejection that preserves the previous file. This is headless file-format evidence; no GTK save action, Ubuntu run, or real runtime export has been verified yet. A saved report remains a historical screened study, not a refreshed KSP observation or a Principia-equivalent result.
