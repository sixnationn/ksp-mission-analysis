# Fixed-impulse report and GTK trial wiring

The dedicated version-1 evaluation report embeds the exact runtime snapshot bytes, submitted `evaluate_route` request and four observed worker events. Save/reload recomputes SHA-256, parses source provenance, replays the client protocol, binds the completed roles/burns/dates/frame/epoch to the submitted trial, checks source atmosphere and request limits, and preserves the partial `independent_nbody_fixed_impulse_checkpointed_only` label with both evidence flags false. An atomic replacement keeps the prior file if validation rejects a new report. This is internal consistency validation; the two checkpoint state histories are absent from the event and the report does not independently recompute their residuals or authenticate who generated an event.

The first report test build failed before its implementation file existed. Later red cases exposed acceptance of identical coarse/strict settings, an overlarge ephemeris fit budget, source-inconsistent Venus clearance, an incorrect Venus disagreement budget, contradictory Mars/Venus disagreement scalars, and extra verification claims. The final tests reject changed source bytes/hash, mismatched roles/impulses/stay/frame/request ID, missing/reordered/partial events, out-of-tolerance residuals, unsafe Venus/Mars margins, altered derived disagreements and unsupported claims. A valid manufactured event stream saves and reloads identically; the worker test also composes a report from its own positive runtime-shaped `evaluate_route` event stream.

The GTK form now accepts a user-supplied fixed-impulse trial JSON file and sends it through the new client kind against the currently loaded exact runtime source. It shows phase progress, four charged burns and checkpoint residuals, Venus/Mars diagnostics, and the explicit checkpointed label. A completed trial can save the self-contained report; cancellation/error cannot. The existing screened-route path and study report remain separate. This is an advanced supplied trial, not a route optimizer or automatic screened-route conversion.

Local evidence: MSYS2 UCRT64 built the GTK desktop and worker with no new dependencies; all **13/13** CTest targets passed. MSVC Debug focused worker/report CTest passed **2/2**, including the worker-produced report composition. The separate report tests use manufactured protocol data, and the worker positive uses a manufactured runtime-shaped system. No GTK window, installed KSP runtime, or Principia comparison was opened or claimed. Rendered review, actual runtime source capture, continuous Mars parking, full route optimization, native packages and benchmark UI responsiveness remain open.

All six Ubuntu and Windows jobs passed for source-bearing commit `f892d31` in [CI run 35807845723](https://github.com/sixnationn/ksp-mission-analysis/actions/runs/35807845723).
