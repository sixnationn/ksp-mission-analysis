# M5 desktop validation checkpoint

22 September 2026. The initial GTK4/gtkmm 3D window and a runtime JSON import slice compile on Windows MSYS2 UCRT64. A window-only 1468 × 909 capture of the synthetic study was inspected: body markers and paths, source/frame/units labels, mission fields, disabled search/export, and candidate/progress area rendered. A marker click selected Ares; drag, pan and zoom changed the camera without changing the selected state. This establishes rendered evidence for the synthetic first slice only.

The import path now reads exact file bytes, checks a caller-supplied SHA-256, validates the Principia runtime schema and prepares a bounded independent Newtonian preview before changing the visible scene. Headless `--validate-snapshot` accepted a runtime-shaped two-body fixture and rejected bad JSON, wrong/missing hash, bad Principia provenance and unsupported frame. A rejected import preserved the labeled synthetic scene in one inspected window capture. The accepted-runtime scene has not been visually inspected because project windows were opening on the user's main Windows desktop; native UI launches were stopped. No live KSP flight export has been accepted.

The runtime reader now formats the validated 365-day no-leap display calendar separately from SI-second UT. Boundary, negative-UT, short-day and invalid-input checks pass in the Windows C++ test target. The GTK source panel compiles with the calendar label and exact-ID JNSQ role suggestions; suggestions remain unconfirmed and cannot start search. This UI change has only headless build evidence because native project windows remain stopped pending reliable virtual-desktop placement.

Still open: real imported-system rendered review, rendered no-leap calendar display, editable mission constraints, separate worker process integration and cancellation in the GTK window, candidate arc and burn displays, save/reload, reports, Ubuntu native rendering, Windows and Ubuntu distributable packaging, and workload responsiveness. The worker and desktop are separate slices today; search/export stay disabled.

[GitHub Actions run 35791955750](https://github.com/sixnationn/ksp-mission-analysis/actions/runs/35791955750) passed the GTK/worker CMake build and all CTest targets on native Ubuntu 24.04 and Windows 2022 UCRT64. The Windows and Ubuntu artifacts are dynamic build-check binaries, not standalone end-user packages.
